# 複製這個檔案成 .env（放在專案根目錄），然後填入你的金鑰。
# .env 已列在 .gitignore，不會被 commit。
#
# ⚠️ 其實**不一定要編輯這個檔案** —— 下面 Azure、Gemini 與 NARRATION_* 那幾個，
# 用擁有者的帳號（這台伺服器上第一個註冊的那個）登入後在「設定 → API 金鑰」與
# 「講評端點」就填得了，存了立刻生效、不用重啟，手機上也可以。
# 那條路存的是 <DATA_DIR>/settings.env，而且**會蓋掉這個檔案裡的同名設定**
# （啟動時先讀 .env、再讀 settings.env）。詳見 README「從設定頁填金鑰 —— 安全邊界」。
#
# 兩組金鑰都是選填的，各自負責不同的事：
#   Azure  → 發音評估的客觀分數（逐字、逐音素）
#   Gemini → 把那些分數講成中文的教練建議
# 都不填的話，除了「跟讀」的評分之外，其他五種模式照樣可以用。

# ─── Azure Speech（發音評估，主要）────────────────────────────────────────
# 提供客觀的逐音素分數：準確度／流暢度／完整度／語調。
# 申請：Azure 入口網站 → 建立「語音服務 (Speech service)」資源 → 金鑰與端點
# REGION 要跟你建立資源時選的區域一致，例如 eastasia、japaneast、westus
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=eastasia

# ─── Gemini（把分數講成中文教練建議，選填）──────────────────────────────
# 沒設定的話會改用本地摘要，Azure 的分數仍然可以看。
# 沒設定 Azure 時，Gemini 會退而自己給主觀分數（比較不準，見 README）。
# 金鑰申請：https://aistudio.google.com/apikey
GEMINI_API_KEY=

# 分析用的預設 model（選填）。值要在 server/gemini.js 的白名單裡，
# 打錯會警告並退回預設值。前端的選單可以隨時改，這只是預設。
# GEMINI_MODEL=gemini-3.6-flash

# ─── 講評換一個更快的模型（選填）────────────────────────────────────────
#
# 設定好 Azure 之後，實際練起來唯一有感的等待就是講評那一段：分數一兩秒就出來，
# 然後對著轉圈圈再等 Gemini 幾秒到十幾秒。而那一段做的事很小 ——
# 把幾個數字寫成四行中文。換一個快的模型就會快很多。
#
# 這四個一起設才會生效，缺一個會退回本地摘要（設定頁會寫出缺哪一個）。
# 端點只要是「OpenAI 相容」的 /chat/completions 就行，所以不綁任何一家。
#
# ⚠️ Hugging Face 有兩種端點，選錯會比 Gemini 還慢：
#   router.huggingface.co/v1     ← 用這個（Inference Providers，沒有冷啟動）
#   api-inference.huggingface.co ← 不要用（舊的 serverless，冷啟動 20 秒以上）
#
# model 的 id 要照供應商自己列的完整填（HF 的 router 可以在 model 後面加
# `:供應商` 指定要轉給誰，例如 `:groq`）。實際可用的 id 請到供應商的
# model 列表複製，不要照抄下面的範例 —— 那些會過期。
#
# NARRATION_PROVIDER=openai
# NARRATION_BASE_URL=https://router.huggingface.co/v1
# NARRATION_API_KEY=hf_xxxxxxxxxxxx
# NARRATION_MODEL=<到 https://huggingface.co/models?inference_provider=all 複製一個>
#
# 其他相容的端點（base URL 的形狀，金鑰各自申請）：
#   Groq    https://api.groq.com/openai/v1
#   Ollama  http://localhost:11434/v1（本機跑，金鑰隨便填一個非空字串）
#
# 填 local 的話完全不呼叫模型，講評一律用本地摘要（最快，但比較呆板）：
# NARRATION_PROVIDER=local

# ─── 帳號（跨裝置同步用）──────────────────────────────────────────────────
#
# 註冊規則：**第一個帳號隨便誰都能建**（那個就是擁有者），之後預設關閉。
# 公開網域上不這樣做的話，任何人都能建帳號來燒你的 Azure 配額。
#
# 要再開一個帳號（家人、另一台裝置用不同帳號）就設這個，註冊時要填對才放行。
# 留空 = 之後完全不開放註冊。
# INVITE_CODE=

# 帳號、session 與學習進度存在哪（選填，預設是專案目錄下的 userdata/）。
#
# ⚠️ **走 Docker 的話這個由映像檔設成 /data，而且一定要掛 volume** ——
# docker-compose.yml 已經掛好了（userdata）。自己改部署方式時千萬別漏掉：
# 沒掛的話容器一重建，使用者的全部學習進度就消失，而且不會有任何錯誤訊息。
# DATA_DIR=./userdata

# 伺服器的埠（選填，預設 3000）
# PORT=3000

# ─── 以下只有用 Docker 部署時才需要（見 README 的「用 Docker 跑在自己的機器上」）───

# 你會用哪個名字連到這台機器：VPN 的內網 IP 或網域。
# Caddy 會照這個值簽憑證，填錯的話瀏覽器會說憑證名稱不符。
# SITE_ADDRESS=10.0.0.5

# 要把埠綁在哪個介面上。填 VPN（WireGuard）介面的 IP，服務就只在 VPN 內看得到。
# 留空預設 0.0.0.0 = 對所有介面開放；這個 App 沒有存取控制，不要就這樣放在有公網的機器上。
# BIND_ADDR=10.0.0.5

# HTTPS 對外的埠。預設 8443（低於 1024 要特權，WSL 上也常被佔用）。
# HTTPS_PORT=8443

# 只有走 Let's Encrypt（Caddyfile 裡的 DNS-01）才需要，二選一：

# DuckDNS 的 token（免費網域，到 https://www.duckdns.org 登入後首頁就看得到）
# DUCKDNS_API_TOKEN=

# 或者自己的網域掛在 Cloudflare：API token，權限給 Zone:DNS:Edit 就好
# CF_API_TOKEN=
