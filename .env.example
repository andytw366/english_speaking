# 複製這個檔案成 .env（放在專案根目錄），然後填入你的金鑰。
# .env 已列在 .gitignore，不會被 commit。

# ─── Azure Speech（發音評估，主要）────────────────────────────────────────
# 提供客觀的逐音素分數：準確度／流暢度／完整度／語調。
# 申請：Azure 入口網站 → 建立「語音服務 (Speech service)」資源 → 金鑰與端點
# REGION 要跟你建立資源時選的區域一致，例如 eastasia、japaneast、westus
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=eastasia

# ─── Gemini（把分數講成中文教練建議，選填）──────────────────────────────
# 沒設定的話會改用本地摘要，Azure 的分數仍然可以看。
# 金鑰申請：https://aistudio.google.com/apikey
GEMINI_API_KEY=

# 選填，預設 3000
# PORT=3000
