# 環境變數 —— 絕對不要 commit
.env
.env.*
!.env.example

node_modules/
npm-debug.log*
.DS_Store
*.log

# Caddy 本機 CA 的根憑證（用 docker compose cp 匯出來裝到手機上的那張）
caddy-root.crt

# ECDICT 的原始 CSV（約 63 MB，npm run build:vocabulary 會自己下載）
ecdict.csv

# 帳號、session 與學習進度（本機開發時的 DATA_DIR 預設值）。
# 裡面有密碼雜湊與 session，絕對不能 commit
userdata/
